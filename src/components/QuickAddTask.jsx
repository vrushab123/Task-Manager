import React from 'react';

const QuickAddTask = () => {
    return (
        <button className="quick-add-task">
            <i className="bi bi-plus-lg"></i>
        </button>
    );
};

export default QuickAddTask;
